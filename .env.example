# Example environment variables for local development.
# Do NOT commit real secrets. Replace placeholders with your own values locally.

# Private key / token (DO NOT put the real value here)
PRIVATE_KEY=replace_with_your_private_key_here

# Example other variables
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NODE_ENV=development

# Notes:
# 1) This file is for local development only. The real .env file should be created on each developer machine
#    and must NOT be committed to the repository. .env is already listed in .gitignore.
# 2) For CI / deployment, store secrets in GitHub Actions Secrets or your hosting provider's secret manager.
#    Example GitHub Actions usage:
#      - name: Use secret
#        env:
#          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
#        run: |
#          echo "Private key length: ${#PRIVATE_KEY}"
